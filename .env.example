# UmrahOps Environment Configuration
# Copy this file to .env and fill in your values
# All VITE_ prefixed variables are exposed to the frontend

# Supabase Auth & Database
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Note: For Lovable projects, use Cloud secrets for sensitive keys
# The following are examples of backend-only secrets (use in edge functions):

# ChatGPT API (for AI risk scoring) - Add via Cloud Secrets
# OPENAI_API_KEY=your-openai-api-key

# Optional: Remote logging endpoint - Add via Cloud Secrets
# LOG_SERVER_URL=your-log-endpoint

# Local SQLite Database Path (default: ./umrahops.db)
# DB_PATH=./umrahops.db
